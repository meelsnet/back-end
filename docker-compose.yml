version: '3.4'

services:
  authentication.api:
    image: authentication.api:${PLATFORM:-linux}-${TAG:-latest}
    build:
      context: .
      dockerfile: src/Services/Authentication/Authentication.API/Dockerfile

  media.api:
    image: media.api:${PLATFORM:-linux}-${TAG:-latest}
    build:
      context: .
      dockerfile: src/Services/Media/Media.API/Dockerfile

